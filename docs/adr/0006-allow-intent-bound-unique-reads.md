# Allow intent-bound unique message reads

A Search Objective stops at bounded Candidate Messages, while a Read Objective may automatically promote a proven Unique Candidate to a Selected Message and retrieve its full content. Multiple candidates require explicit Operator selection, and full-message authority does not authorize attachment download or any downstream action; this removes redundant confirmation for precise read requests while preserving a boundary for ambiguous and search-only requests.
